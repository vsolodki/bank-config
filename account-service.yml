spring:
  application:
    name: account-service
  thymeleaf:
    cache: false
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
